class ContactCotroller {
    // Listar todos os registros \\
    index(req, res) {
        res.send('send from Contatct Controllers');
    }

    // Obter um registro \\
    show() {}

    // Cadastrar um novo registro \\
    store() {}

    // Atualizar um registro \\
    update() {}

    // Deletar um registro \\
    delete() {}
}

module.exports = new ContactCotroller();
